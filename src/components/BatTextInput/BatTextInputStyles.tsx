import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputer: {
    backgroundColor: '#e5bf3c',
    color: 'black',
    fontSize: 20,
    width: '100%',
    height: 40,
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
    textAlign: 'center'
  }
});