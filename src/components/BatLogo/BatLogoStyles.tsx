import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 30,
    color: '#E5BF3C'
  },
  image: {
    resizeMode: 'contain', 
    height: 180
  }
  
});